name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compiler version
        run: gcc --version

      - name: Build
        run: make holodeck

      - name: Test
        run: make test
